using UnityEngine;

public class Varitable : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int number; // [1] 정수형 변수 선언.
        number = 256; // [2] 변수에 256값 대입(할당)하기.
        Debug.Log(number);

    }

 
}
